/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'www.comediva.com', 'media.tenor.com', 'i1.sndcdn.com'],
    }
}

module.exports = nextConfig
